#!/usr/bin/python
#-*- coding: utf-8 -*-

#Version 2.0
#Author: Castillo Medina Aarón Martín.

def real_mutation(chromosome,mutation_options):
    mutation_probability = mutation_options[0]
    vector_variables = mutation_options[1]
    mutated_chromosome = []
          
    for x in range(len(chromosome)):
        gen = chromosome[x]
        gen_range =  
        number = aleatorio.random()
             
        if number <= mutation_probability:
            
        else:
            mutated_chromosome += [gen]       
             
    return mutated_chromosome
    
